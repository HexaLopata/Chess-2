using UnityEngine;

public class MainFieldFigure : MonoBehaviour
{
    private FigureData _data;
    private Cell _cell;

 // OnClick {
    // if GetParentComponent<Field>().currentTurn == _data.team
    // {
    //      GetParentComponent<Field>().currentFigure = this
    //      cells = GetParentComponent<Field>().cells
    //      ... Выборка нужных клеток
    //      GetParentComponent<Field>().ActivateCells(cells)
    //      
    // }
 // }
}
